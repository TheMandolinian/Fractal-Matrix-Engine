[package]
name = "fme-sra"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Singularity Root Artifact canonical semantics for the Fractal Matrix Engine"

[dependencies]
fme-crypto = { path = "../fme-crypto" }
fme-fer = { path = "../fme-fer" }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
